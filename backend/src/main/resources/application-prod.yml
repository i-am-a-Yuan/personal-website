# 生产环境配置
spring:
  application:
    name: personal-website
  
  # MySQL 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/personal_website?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: testUser
    password: StrongPass123!
  
  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

# 服务器配置
server:
  port: 8080
  address: 0.0.0.0

# JWT 配置
jwt:
  secret: your-production-secret-key-change-this-in-production
  expiration: 86400000

# 日志配置
logging:
  level:
    root: INFO
    com.personal.website: INFO
  file:
    name: /var/log/supervisor/application.log